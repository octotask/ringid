# Makefile for static site deployment (ringid.com)
.PHONY: build deploy

# No build step by default; add minification or bundling if needed
build:
	@echo "No build step needed for static assets. Add minification or bundling if desired."

deploy:
	@echo "Deploying static site..."
	# Example: rsync to server or copy to deployment directory
	# rsync -av content/ user@server:/var/www/html/
